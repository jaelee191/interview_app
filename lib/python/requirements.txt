# PDF 처리 라이브러리 (필수)
pypdf2==3.0.1  # PDF 기본 처리
pdfplumber==0.10.3  # 고급 PDF 텍스트 추출
pymupdf==1.23.8  # 빠른 PDF 처리 (fitz)

# 텍스트 처리 및 분석
chardet==5.2.0  # 인코딩 자동 감지
python-docx==1.1.0  # Word 문서 처리 (선택)

# 한글 자연어 처리 라이브러리 (선택사항)
kiwipiepy==0.14.1  # Kiwi (순수 Python, 설치 쉬움)

# 선택적 고급 라이브러리
# konlpy==0.6.0  # 한글 형태소 분석기 모음 (Java 필요)
# soynlp==0.0.493  # 비지도 학습 기반 한글 처리
# pykospacing==0.5  # 딥러닝 기반 띄어쓰기