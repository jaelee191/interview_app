# 한글 자연어 처리 라이브러리 (선택사항)

# KoNLPy - 한글 형태소 분석기 모음
# konlpy==0.6.0

# 형태소 분석기 (택 1)
# mecab-python3==1.0.5  # Mecab (가장 빠름)
# python-mecab-ko==1.0.14  # Mecab 한국어 특화
# kiwipiepy==0.14.1  # Kiwi (순수 Python, 설치 쉬움)

# 한글 전처리
# soynlp==0.0.493  # 비지도 학습 기반 한글 처리
# hangul-utils==0.5.5  # 한글 자모 분리/결합

# 띄어쓰기 교정
# pykospacing==0.5  # 딥러닝 기반 띄어쓰기
# py-hanspell==1.1  # 네이버 맞춤법 검사기

# 현재는 표준 라이브러리만 사용 중
# 필요시 위 라이브러리들을 선택적으로 설치