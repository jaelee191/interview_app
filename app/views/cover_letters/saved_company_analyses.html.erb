<div class="min-h-screen bg-gradient-to-br from-indigo-50 to-purple-100 py-12">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-12">
      <h1 class="text-4xl font-bold text-gray-900 mb-4">
        📚 내 기업 분석 목록
      </h1>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        분석한 기업 리포트를 관리하고 다시 확인할 수 있습니다
      </p>
    </div>

    <!-- Action Buttons -->
    <div class="flex justify-between items-center mb-8">
      <a href="<%= company_analysis_cover_letters_path %>" 
         class="inline-flex items-center gap-2 px-4 py-2 bg-white hover:bg-gray-50 text-gray-700 font-medium rounded-lg shadow transition-colors">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
        </svg>
        새 분석 시작
      </a>
      
      <div class="text-sm text-gray-600">
        총 <span class="font-bold text-indigo-600"><%= @company_analyses.total_count %></span>개의 분석 저장됨
      </div>
    </div>

    <!-- Saved Analyses List -->
    <% if @company_analyses.any? %>
      <div class="grid gap-6">
        <% @company_analyses.each do |analysis| %>
          <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow">
            <div class="p-6">
              <div class="flex items-start justify-between">
                <div class="flex-1">
                  <div class="flex items-center gap-3 mb-3">
                    <h2 class="text-2xl font-bold text-gray-900">
                      <%= analysis.company_name %>
                    </h2>
                    <% if analysis.metadata&.dig('analysis_type') == 'enhanced_professional' %>
                      <span class="px-3 py-1 bg-purple-100 text-purple-700 text-sm font-medium rounded-full">
                        Professional
                      </span>
                    <% elsif analysis.metadata&.dig('analysis_type') == 'job_seeker_focused' %>
                      <span class="px-3 py-1 bg-green-100 text-green-700 text-sm font-medium rounded-full">
                        구직자 맞춤
                      </span>
                    <% else %>
                      <span class="px-3 py-1 bg-blue-100 text-blue-700 text-sm font-medium rounded-full">
                        Standard
                      </span>
                    <% end %>
                    <% if analysis.saved %>
                      <span class="px-3 py-1 bg-yellow-100 text-yellow-700 text-sm font-medium rounded-full">
                        ⭐ 저장됨
                      </span>
                    <% end %>
                  </div>
                  
                  <div class="grid md:grid-cols-3 gap-4 mb-4">
                    <div>
                      <span class="text-sm text-gray-500">산업</span>
                      <p class="font-medium text-gray-900"><%= analysis.industry || '미분류' %></p>
                    </div>
                    <div>
                      <span class="text-sm text-gray-500">기업 규모</span>
                      <p class="font-medium text-gray-900"><%= analysis.company_size || '미상' %></p>
                    </div>
                    <div>
                      <span class="text-sm text-gray-500">분석일</span>
                      <p class="font-medium text-gray-900"><%= analysis.analysis_date.strftime("%Y년 %m월 %d일") %></p>
                    </div>
                  </div>
                  
                  <% if analysis.metadata&.dig('executive_summary').present? %>
                    <div class="bg-gray-50 rounded-lg p-4 mb-4">
                      <h3 class="text-sm font-semibold text-gray-700 mb-2">Executive Summary</h3>
                      <p class="text-sm text-gray-600 line-clamp-3">
                        <%= strip_tags(analysis.metadata['executive_summary']).truncate(200) %>
                      </p>
                    </div>
                  <% end %>
                  
                  <div class="flex items-center gap-2 text-sm">
                    <% if analysis.cached_until.present? && analysis.cached_until > Time.current %>
                      <span class="inline-flex items-center gap-1 text-green-600">
                        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                        </svg>
                        캐시 유효 (<%= analysis.cached_until.strftime("%m/%d까지") %>)
                      </span>
                    <% else %>
                      <span class="inline-flex items-center gap-1 text-gray-500">
                        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
                        </svg>
                        캐시 만료
                      </span>
                    <% end %>
                  </div>
                </div>
                
                <div class="flex gap-2 ml-4">
                  <a href="<%= company_analysis_result_cover_letters_path(analysis) %>" 
                     class="px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-medium rounded-lg transition-colors text-center">
                    결과 보기
                  </a>
                  <button onclick="deleteAnalysis(<%= analysis.id %>, '<%= analysis.company_name %>')" 
                          class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white text-sm font-medium rounded-lg transition-colors"
                          style="background-color: #dc2626; color: white;">
                    삭제
                  </button>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
      
      <!-- Pagination -->
      <div class="mt-8 flex justify-center">
        <%= paginate @company_analyses %>
      </div>
    <% else %>
      <!-- Empty State -->
      <div class="bg-white rounded-xl shadow-md p-12">
        <div class="text-center">
          <svg class="mx-auto h-12 w-12 text-gray-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
          <h3 class="text-lg font-medium text-gray-900 mb-2">저장된 분석이 없습니다</h3>
          <p class="text-gray-500 mb-6">새로운 기업 분석을 시작하고 저장해보세요</p>
          <a href="<%= company_analysis_cover_letters_path %>" 
             class="inline-flex items-center gap-2 px-6 py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-medium rounded-lg transition-colors">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
            첫 분석 시작하기
          </a>
        </div>
      </div>
    <% end %>
  </div>
</div>

<style>
.line-clamp-3 {
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
</style>

<script>
function deleteAnalysis(id, companyName) {
  if (confirm(`정말로 "${companyName}" 기업 분석을 삭제하시겠습니까?\n\n이 작업은 되돌릴 수 없습니다.`)) {
    fetch(`/cover_letters/delete_company_analysis/${id}`, {
      method: 'DELETE',
      headers: {
        'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content,
        'Content-Type': 'application/json',
        'Accept': 'application/json'
      }
    })
    .then(response => response.json())
    .then(data => {
      if (data.success) {
        alert('분석이 성공적으로 삭제되었습니다.');
        window.location.reload();
      } else {
        alert('삭제 실패: ' + (data.error || '알 수 없는 오류'));
      }
    })
    .catch(error => {
      console.error('Error:', error);
      alert('삭제 중 오류가 발생했습니다.');
    });
  }
}
</script>