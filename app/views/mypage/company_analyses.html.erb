<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- 페이지 헤더 -->
  <div class="bg-white rounded-lg border border-gray-100 p-6 mb-6">
    <div class="flex items-center justify-between">
      <div>
        <div class="flex items-center gap-3 mb-2">
          <%= link_to "마이페이지", mypage_path, class: "text-gray-500 hover:text-gray-700" %>
          <span class="text-gray-400">/</span>
          <span class="text-gray-900 font-medium">저장된 기업분석</span>
        </div>
        <h1 class="text-2xl font-semibold text-gray-900">저장된 기업 분석</h1>
        <p class="text-gray-600 mt-2">분석했던 기업 정보를 다시 확인할 수 있습니다</p>
      </div>
      <%= link_to "새 기업 분석", company_analysis_cover_letters_path, 
          class: "px-4 py-2 bg-emerald-500 text-white rounded-lg hover:bg-emerald-600 transition-colors" %>
    </div>
  </div>
  
  <!-- 기업분석 목록 -->
  <% if @company_analyses.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      <% @company_analyses.each do |analysis| %>
        <div class="bg-white rounded-lg border border-gray-100 p-6 hover:shadow-md transition-shadow">
          <div class="mb-4">
            <h3 class="text-lg font-medium text-gray-900 mb-1">
              <%= analysis.company_name %>
            </h3>
            <% if analysis.industry.present? %>
              <p class="text-emerald-600 font-medium">
                <%= analysis.industry %>
              </p>
            <% end %>
          </div>
          
          <% if analysis.company_size.present? %>
            <div class="mb-3">
              <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                <%= analysis.company_size %>
              </span>
            </div>
          <% end %>
          
          <div class="flex items-center justify-between text-sm text-gray-500 mb-4">
            <span>분석일: <%= analysis.analysis_date&.strftime("%Y.%m.%d") || analysis.created_at.strftime("%Y.%m.%d") %></span>
          </div>
          
          <div class="flex items-center gap-2">
            <%= link_to "분석 결과 보기", company_analysis_result_cover_letters_path(analysis), 
                class: "flex-1 text-center px-3 py-1.5 text-sm text-white bg-emerald-500 rounded hover:bg-emerald-600 transition-colors" %>
            <%= link_to "삭제", delete_company_analysis_cover_letters_path(analysis), 
                data: { "turbo-method": :delete, turbo_confirm: "정말 삭제하시겠습니까?" },
                class: "px-3 py-1.5 text-sm text-red-600 hover:text-red-700 transition-colors" %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="bg-white rounded-lg border border-gray-100 p-12 text-center">
      <div class="text-gray-400 mb-4">
        <svg class="w-16 h-16 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" 
                d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
        </svg>
      </div>
      <h3 class="text-lg font-medium text-gray-900 mb-2">저장된 기업분석이 없습니다</h3>
      <p class="text-gray-600 mb-6">관심있는 기업을 분석하고 저장해보세요</p>
      <%= link_to "기업 분석하기", company_analysis_cover_letters_path, 
          class: "inline-flex items-center px-4 py-2 bg-emerald-500 text-white rounded-lg hover:bg-emerald-600 transition-colors" %>
    </div>
  <% end %>
</div>