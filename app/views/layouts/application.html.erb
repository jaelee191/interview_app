<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "AI 자소서 도우미 - 스마트한 취업 준비의 시작" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="AI가 분석하는 맞춤형 자기소개서 작성 도우미. 채용공고 분석부터 자소서 첨삭까지 한 번에!">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <!-- Pretendard Font -->
    <link rel="stylesheet" as="style" crossorigin href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css" />

    <%# Include CSS files without preload %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload", media: "all" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: "all" %>
    <%= stylesheet_link_tag "modern", "data-turbo-track": "reload", media: "all" %>
    
    <%# JavaScript with importmap %>
    <%= javascript_importmap_tags %>
    
    <style>
      :root {
        --primary-color: #10b981;
        --primary-hover: #059669;
        --text-primary: #111827;
        --text-secondary: #6B7280;
        --border-color: #f3f4f6;
      }
    </style>
    
    <%= yield :extra_css %>
  </head>

  <body class="h-full bg-gray-50 font-[Pretendard]">
    <!-- Modern Navigation -->
    <nav class="fixed top-0 w-full bg-white border-b border-gray-100 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- Logo -->
          <div class="flex items-center">
            <%= link_to root_path, class: "flex flex-col items-start group hover:opacity-80 transition-opacity" do %>
              <span class="text-xl font-semibold text-gray-900">jachui</span>
              <span class="text-xs text-gray-500">스토리 기반 자소서 작성 플랫폼</span>
            <% end %>
          </div>
          
          <!-- Navigation Links -->
          <div class="hidden md:flex items-center space-x-6">
            <!-- 서비스 소개 드롭다운 -->
            <div class="relative group">
              <button class="text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium flex items-center">
                서비스 소개
                <svg class="w-3 h-3 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                </svg>
              </button>
              <div class="absolute top-full left-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50">
                <%= link_to "서비스 철학", about_path, class: "block px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50" %>
                <%= link_to "서비스를 만든 이유", letter_path, class: "block px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50 border-t border-gray-100" %>
                <%= link_to "자소서 작성 가이드", guide_cover_letters_path, class: "block px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50 border-t border-gray-100" %>
                <%= link_to "성공 사례", "#", class: "block px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50 border-t border-gray-100" %>
              </div>
            </div>
            
            <!-- 채용공고 분석 -->
            <%= link_to "채용공고 분석", job_posting_cover_letters_path,
                class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
            
            <!-- 기업 분석 -->
            <%= link_to "기업 분석", company_analysis_cover_letters_path,
                class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
            
            <!-- 나만의 스토리 찾기 -->
            <%= link_to "나만의 스토리 찾기", interactive_cover_letters_path, 
                class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
            
            <!-- 내 자소서 분석하기 -->
            <%= link_to "내 자소서 분석하기", advanced_cover_letters_path, 
                class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
            
            <% if user_signed_in? %>
              <%= link_to "무료 크레딧 받기", referral_index_path, class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
            <% else %>
              <%= link_to "무료 크레딧 받기", referral_path, class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
            <% end %>
          </div>
          
          <!-- Auth Buttons & Mobile Menu -->
          <div class="flex items-center gap-4">
            <% if defined?(current_user) && current_user %>
              <!-- 마이페이지 햄버거 메뉴 (데스크톱 & 모바일) -->
              <div class="relative group">
                <button class="p-2 text-gray-600 hover:text-gray-900 transition-colors">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                  </svg>
                </button>
                <div class="absolute top-full right-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50">
                  <div class="px-4 py-3 border-b border-gray-100">
                    <p class="text-sm font-medium text-gray-900">마이페이지</p>
                  </div>
                  <% if current_user&.admin? %>
                    <%= link_to "관리자", admin_root_path, class: "block px-4 py-2.5 text-sm text-emerald-700 hover:bg-emerald-50 font-medium" %>
                    <div class="border-t border-gray-100"></div>
                  <% end %>
                  <%= link_to "대시보드", mypage_path, class: "block px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50" %>
                  <%= link_to "친구 추천", referral_index_path, class: "block px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50" %>
                  <%= link_to "요금제", pricing_path, class: "block px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50" %>
                  <%= link_to "내 프로필", user_profile_path(current_user.id), class: "block px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50" %>
                  <%= link_to "계정 설정", edit_user_registration_path, class: "block px-4 py-2.5 text-sm text-gray-700 hover:bg-gray-50" %>
                  <div class="border-t border-gray-200 mt-2 pt-2">
                    <%= link_to "로그아웃", destroy_user_session_path, data: { "turbo-method": :delete }, class: "block px-4 py-2.5 text-sm text-red-600 hover:bg-red-50 rounded-b-lg" %>
                  </div>
                </div>
              </div>
            <% else %>
              <!-- 비로그인 시 버튼들 -->
              <div class="hidden md:flex items-center gap-3">
                <%= link_to "로그인", new_user_session_path, class: "text-gray-600 hover:text-gray-900 text-sm font-medium transition-colors" %>
                <%= link_to "시작하기", new_user_registration_path, class: "px-4 py-2 bg-emerald-500 text-white text-sm font-medium rounded-full hover:bg-emerald-600 transition-colors" %>
              </div>
              <!-- 모바일 메뉴 버튼 (비로그인) -->
              <button id="mobileMenuBtn" class="md:hidden p-2 text-gray-600 hover:text-gray-900">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
              </button>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    
    <!-- Mobile Menu (비로그인 사용자용) -->
    <% unless defined?(current_user) && current_user %>
      <div id="mobileMenu" class="hidden md:hidden fixed top-16 left-0 w-full bg-white border-b border-gray-100 shadow-lg z-40">
        <div class="px-4 py-4 space-y-2">
          <!-- 서비스 소개 섹션 -->
          <div>
            <button class="w-full flex items-center justify-between px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg" onclick="toggleMobileSubmenu('serviceSubmenu')">
              서비스 소개
              <svg class="w-4 h-4 transform transition-transform" id="serviceArrow" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
            <div id="serviceSubmenu" class="hidden ml-4 mt-2 space-y-1">
              <%= link_to "서비스 철학", about_path, class: "block px-3 py-2 text-sm text-gray-600 hover:bg-gray-50 rounded" %>
              <%= link_to "서비스를 만든 이유", letter_path, class: "block px-3 py-2 text-sm text-gray-600 hover:bg-gray-50 rounded" %>
              <%= link_to "자소서 작성 가이드", guide_cover_letters_path, class: "block px-3 py-2 text-sm text-gray-600 hover:bg-gray-50 rounded" %>
              <%= link_to "성공 사례", "#", class: "block px-3 py-2 text-sm text-gray-600 hover:bg-gray-50 rounded" %>
            </div>
          </div>
          
          <%= link_to "채용공고 분석", job_posting_cover_letters_path, class: "block px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg" %>
          <%= link_to "기업 분석", company_analysis_cover_letters_path, class: "block px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg" %>
          <%= link_to "AI 자소서 작성", interactive_cover_letters_path, class: "block px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg" %>
          <%= link_to "심층 분석", advanced_cover_letters_path, class: "block px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg" %>
          <%= link_to "요금제", pricing_path, class: "block px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg" %>
          <%= link_to "친구 추천", referral_path, class: "block px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg" %>
          
          <!-- Auth Links -->
          <div class="border-t border-gray-100 pt-2 mt-2">
            <%= link_to "로그인", new_user_session_path, class: "block px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 rounded-lg" %>
            <%= link_to "시작하기", new_user_registration_path, class: "block px-3 py-2 text-sm font-medium text-white bg-emerald-500 hover:bg-emerald-600 rounded-lg text-center" %>
          </div>
        </div>
      </div>
    <% end %>
    
    <!-- Main Content -->
    <main class="pt-16">
      <%= yield %>
    </main>
    
    <!-- Modern Footer -->
    <footer class="bg-white border-t border-gray-100 mt-24">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
        <div class="grid md:grid-cols-4 gap-8 mb-12">
          <!-- Company Info -->
          <div class="col-span-2 md:col-span-1">
            <span class="text-xl font-semibold text-gray-900">jachui</span>
            <p class="text-sm text-gray-600 mt-4 leading-relaxed">
              AI는 도구일 뿐, 진짜 이야기는<br>
              지원자 본인만이 쓸 수 있습니다
            </p>
          </div>
          
          <!-- Services -->
          <div>
            <h4 class="text-sm font-semibold text-gray-900 mb-4">서비스</h4>
            <ul class="space-y-2">
              <li><a href="/cover_letters/job_posting" class="text-sm text-gray-600 hover:text-gray-900 transition-colors">채용공고 분석</a></li>
              <li><a href="/cover_letters/company_analysis" class="text-sm text-gray-600 hover:text-gray-900 transition-colors">기업 분석</a></li>
              <li><a href="/cover_letters/interactive" class="text-sm text-gray-600 hover:text-gray-900 transition-colors">AI 자소서 작성</a></li>
              <li><a href="/cover_letters/advanced" class="text-sm text-gray-600 hover:text-gray-900 transition-colors">심층 분석</a></li>
              <li><a href="/cover_letters" class="text-sm text-gray-600 hover:text-gray-900 transition-colors">이력 관리</a></li>
            </ul>
          </div>
          
          <!-- Resources -->
          <div>
            <h4 class="text-sm font-semibold text-gray-900 mb-4">리소스</h4>
            <ul class="space-y-2">
              <li><a href="/about" class="text-sm text-gray-600 hover:text-gray-900 transition-colors">회사 소개</a></li>
              <li><a href="#" class="text-sm text-gray-600 hover:text-gray-900 transition-colors">자소서 가이드</a></li>
              <li><a href="#" class="text-sm text-gray-600 hover:text-gray-900 transition-colors">합격 사례</a></li>
              <li><a href="#" class="text-sm text-gray-600 hover:text-gray-900 transition-colors">FAQ</a></li>
            </ul>
          </div>
        </div>
        
        <!-- Bottom Bar -->
        <div class="pt-8 border-t border-gray-100">
          <div class="flex flex-col md:flex-row justify-between items-center">
            <div class="mb-4 md:mb-0">
              <p class="text-sm text-gray-500">© 2025 jachui. All rights reserved.</p>
            </div>
            <div class="flex space-x-6">
              <a href="#" class="text-sm text-gray-500 hover:text-gray-900 transition-colors">이용약관</a>
              <a href="#" class="text-sm text-gray-500 hover:text-gray-900 transition-colors">개인정보처리방침</a>
              <a href="#" class="text-sm text-gray-500 hover:text-gray-900 transition-colors">문의하기</a>
            </div>
          </div>
        </div>
      </div>
    </footer>
    <!-- JavaScript for Mobile Menu -->
    <script>
      // Mobile menu toggle
      document.getElementById('mobileMenuBtn')?.addEventListener('click', function() {
        const mobileMenu = document.getElementById('mobileMenu');
        const isHidden = mobileMenu.classList.contains('hidden');
        
        if (isHidden) {
          mobileMenu.classList.remove('hidden');
          // Change hamburger to X
          this.innerHTML = '<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>';
        } else {
          mobileMenu.classList.add('hidden');
          // Change X to hamburger
          this.innerHTML = '<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>';
        }
      });
      
      // Mobile submenu toggle
      function toggleMobileSubmenu(submenuId) {
        const submenu = document.getElementById(submenuId);
        const arrow = document.getElementById(submenuId.replace('Submenu', 'Arrow'));
        
        if (submenu.classList.contains('hidden')) {
          // Close all other submenus first
          document.querySelectorAll('[id$="Submenu"]').forEach(menu => {
            if (menu.id !== submenuId) {
              menu.classList.add('hidden');
              const otherArrow = document.getElementById(menu.id.replace('Submenu', 'Arrow'));
              if (otherArrow) otherArrow.classList.remove('rotate-180');
            }
          });
          
          // Open this submenu
          submenu.classList.remove('hidden');
          arrow.classList.add('rotate-180');
        } else {
          // Close this submenu
          submenu.classList.add('hidden');
          arrow.classList.remove('rotate-180');
        }
      }
      
      // Close mobile menu when clicking outside
      document.addEventListener('click', function(e) {
        const mobileMenu = document.getElementById('mobileMenu');
        const mobileMenuBtn = document.getElementById('mobileMenuBtn');
        
        if (mobileMenu && mobileMenuBtn && !mobileMenu.contains(e.target) && !mobileMenuBtn.contains(e.target)) {
          if (!mobileMenu.classList.contains('hidden')) {
            mobileMenu.classList.add('hidden');
            mobileMenuBtn.innerHTML = '<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>';
          }
        }
      });
    </script>
  </body>
</html>
