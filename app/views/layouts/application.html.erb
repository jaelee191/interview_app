<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "뉴스 크롤러 - 기업 인텔리전스 플랫폼" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <!-- Inter Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <style>
      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      }
    </style>
  </head>

  <body class="h-full bg-white">
    <!-- Navigation -->
    <nav class="fixed top-0 w-full bg-white/95 backdrop-blur-md border-b border-gray-100 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- Logo -->
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center space-x-2" do %>
              <div class="w-8 h-8 bg-black rounded-lg flex items-center justify-center">
                <span class="text-white font-bold text-sm">N</span>
              </div>
              <span class="text-xl font-semibold text-gray-900">뉴스크롤러</span>
            <% end %>
          </div>
          
          <!-- Navigation Links -->
          <div class="hidden md:flex items-center space-x-8">
            <%= link_to "기업 검색", companies_path, class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
            <%= link_to "뉴스 기사", articles_path, class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
            <%= link_to "자소서 분석", cover_letters_path, class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
            <%= link_to "통합 검색", search_companies_path, class: "text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium" %>
          </div>
          
          <!-- CTA Button -->
          <div class="flex items-center space-x-4">
            <%= link_to "크롤링 시작", companies_path, class: "bg-black text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-gray-800 transition-colors" %>
          </div>
        </div>
      </div>
    </nav>
    
    <!-- Main Content -->
    <main class="pt-16">
      <%= yield %>
    </main>
    
    <!-- Footer -->
    <footer class="border-t border-gray-100 mt-24">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="flex flex-col md:flex-row justify-between items-center">
          <div class="mb-4 md:mb-0">
            <p class="text-sm text-gray-500">© 2025 뉴스크롤러. 모든 권리 보유.</p>
          </div>
          <div class="flex space-x-6">
            <span class="text-sm text-gray-400">레일스 8.0.2</span>
            <span class="text-sm text-gray-400">포트 3004</span>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
